#include <stdio.h>

int main() {
	printf("Hello World\n");/*printf is a function stored in <stdio.h> (standard input output) header file and the thing which we need to output is written inside (" ")*/
	return 0;
}
